# Calendar Automation

Guide for automating based on calendar data in Home Assistant.

## Overview

Calendar automation enables:
- Scheduling via calendar instead of YAML
- Integration with Google Calendar, CalDAV, etc.
- Dynamic schedules that are easy to change
- Vacation and public holiday handling

## Calendar Platforms

| Platform | Description | Local |
|----------|-------------|-------|
| Local Calendar | Built-in HA calendar | Yes |
| Google Calendar | Google integration | No |
| CalDAV | iCloud, Nextcloud, etc. | Depends |
| ICS | iCal files | Yes |
| Waste Collection | Waste pickup schedule | Yes |

## Local Calendar

### Create Local Calendar

Settings → Devices & Services → Add Integration → Local Calendar

```yaml
# Automatically created entity:
# calendar.my_schedule
```

### Add Event via Service

```yaml
# Generated by aurora@aurora-smart-home (home-assistant skill) v1.7.7
# https://github.com/tonylofgren/aurora-smart-home

automation:
  - alias: "Add vacation"
    id: add_vacation_event
    trigger:
      - platform: state
        entity_id: input_boolean.vacation_mode
        to: "on"
    action:
      - service: calendar.create_event
        target:
          entity_id: calendar.vacation
        data:
          summary: "Vacation"
          start_date: "{{ now().date().isoformat() }}"
          end_date: "{{ (now() + timedelta(days=7)).date().isoformat() }}"
```

## Calendar Trigger

### Basic Calendar Trigger

```yaml
automation:
  - alias: "Calendar - Event starts"
    id: calendar_event_start
    trigger:
      - platform: calendar
        entity_id: calendar.arbetsschema
        event: start
    action:
      - service: switch.turn_on
        target:
          entity_id: switch.office_lights
      - service: climate.set_temperature
        target:
          entity_id: climate.office
        data:
          temperature: 22
```

### Event Ends

```yaml
automation:
  - alias: "Calendar - Event ends"
    id: calendar_event_end
    trigger:
      - platform: calendar
        entity_id: calendar.arbetsschema
        event: end
    action:
      - service: switch.turn_off
        target:
          entity_id: switch.office_lights
      - service: climate.set_preset_mode
        target:
          entity_id: climate.office
        data:
          preset_mode: away
```

### Offset Trigger

Trigger before/after an event:

```yaml
automation:
  - alias: "Calendar - 30 min before meeting"
    id: calendar_before_meeting
    trigger:
      - platform: calendar
        entity_id: calendar.work
        event: start
        offset: "-00:30:00"  # 30 minutes before
    condition:
      - condition: template
        value_template: "{{ 'meeting' in trigger.calendar_event.summary.lower() }}"
    action:
      - service: notify.mobile_app_tonys_iphone
        data:
          title: "Meeting soon"
          message: "{{ trigger.calendar_event.summary }} starts in 30 minutes"
```

## Trigger Data

Available data in `trigger.calendar_event`:

```yaml
automation:
  - alias: "Calendar - Show event data"
    id: calendar_event_data
    trigger:
      - platform: calendar
        entity_id: calendar.home
        event: start
    action:
      - service: notify.mobile_app_tonys_iphone
        data:
          title: "{{ trigger.calendar_event.summary }}"
          message: >
            Start: {{ trigger.calendar_event.start }}
            End: {{ trigger.calendar_event.end }}
            Description: {{ trigger.calendar_event.description | default('None') }}
            Location: {{ trigger.calendar_event.location | default('None') }}
```

## Condition Checks with Calendar

### State-based

```yaml
automation:
  - alias: "Only during work hours"
    id: only_work_hours
    trigger:
      - platform: state
        entity_id: binary_sensor.motion
        to: "on"
    condition:
      # Check if there is an active event
      - condition: state
        entity_id: calendar.work_schedule
        state: "on"
    action:
      - service: light.turn_on
        target:
          entity_id: light.office
```

### Template-based

```yaml
automation:
  - alias: "Check specific event"
    id: check_specific_event
    trigger:
      - platform: time_pattern
        minutes: "/30"
    condition:
      - condition: template
        value_template: >
          {% set cal = state_attr('calendar.work_schedule', 'message') %}
          {{ cal is not none and 'remote work' in cal.lower() }}
    action:
      - service: light.turn_on
        target:
          entity_id: light.office
```

## Vacation Mode

### Automatic Vacation from Calendar

```yaml
automation:
  - alias: "Vacation - Activate"
    id: vacation_start
    trigger:
      - platform: calendar
        entity_id: calendar.vacation
        event: start
    action:
      - service: input_boolean.turn_on
        target:
          entity_id: input_boolean.vacation_mode
      - service: climate.set_preset_mode
        target:
          entity_id: all
        data:
          preset_mode: away
      - service: notify.all_phones
        data:
          title: "Vacation mode activated"
          message: "The house is now in vacation mode"

  - alias: "Vacation - Deactivate"
    id: vacation_end
    trigger:
      - platform: calendar
        entity_id: calendar.vacation
        event: end
    action:
      - service: input_boolean.turn_off
        target:
          entity_id: input_boolean.vacation_mode
      - service: climate.set_preset_mode
        target:
          entity_id: all
        data:
          preset_mode: home
```

## Work Schedule

### Different Schedules per Day

```yaml
# Create events in the calendar:
# - "Morning" 06:00-08:00 (Monday-Friday)
# - "Work hours" 08:00-17:00 (Monday-Friday)
# - "Evening" 17:00-22:00 (every day)
# - "Night" 22:00-06:00 (every day)

automation:
  - alias: "Schedule - Morning routine"
    id: schedule_morning
    trigger:
      - platform: calendar
        entity_id: calendar.daily_schedule
        event: start
    condition:
      - condition: template
        value_template: "{{ trigger.calendar_event.summary == 'Morning' }}"
    action:
      - service: scene.turn_on
        target:
          entity_id: scene.morning

  - alias: "Schedule - Night mode"
    id: schedule_night
    trigger:
      - platform: calendar
        entity_id: calendar.daily_schedule
        event: start
    condition:
      - condition: template
        value_template: "{{ trigger.calendar_event.summary == 'Night' }}"
    action:
      - service: scene.turn_on
        target:
          entity_id: scene.night
```

## Waste Collection

### Waste Collection Schedule

```yaml
# With the Waste Collection Schedule integration

automation:
  - alias: "Waste collection - Reminder"
    id: waste_reminder
    trigger:
      - platform: calendar
        entity_id: calendar.waste_collection
        event: start
        offset: "-18:00:00"  # The evening before
    action:
      - service: notify.all_phones
        data:
          title: "Waste collection tomorrow"
          message: "{{ trigger.calendar_event.summary }}"
      - service: tts.speak
        target:
          entity_id: tts.piper
        data:
          media_player_entity_id: media_player.kitchen
          message: "Remember to put out {{ trigger.calendar_event.summary }} tonight"
```

## Dynamic Schedules

### Fetch Upcoming Events

```yaml
template:
  - sensor:
      - name: "Next calendar event"
        unique_id: next_calendar_event
        state: "{{ state_attr('calendar.home', 'message') | default('None') }}"
        attributes:
          start: "{{ state_attr('calendar.home', 'start_time') }}"
          end: "{{ state_attr('calendar.home', 'end_time') }}"
          location: "{{ state_attr('calendar.home', 'location') }}"
          description: "{{ state_attr('calendar.home', 'description') }}"
```

### Automatic Lighting Based on Event

```yaml
automation:
  - alias: "Dynamic lighting per event"
    id: dynamic_lighting_by_event
    trigger:
      - platform: calendar
        entity_id: calendar.home
        event: start
    action:
      - choose:
          # Movie night
          - conditions:
              - condition: template
                value_template: "{{ 'movie' in trigger.calendar_event.summary.lower() }}"
            sequence:
              - service: scene.turn_on
                target:
                  entity_id: scene.movie

          # Dinner
          - conditions:
              - condition: template
                value_template: "{{ 'dinner' in trigger.calendar_event.summary.lower() }}"
            sequence:
              - service: scene.turn_on
                target:
                  entity_id: scene.dinner

          # Party
          - conditions:
              - condition: template
                value_template: "{{ 'party' in trigger.calendar_event.summary.lower() }}"
            sequence:
              - service: scene.turn_on
                target:
                  entity_id: scene.party
```

## Google Calendar

### Integration Setup

```yaml
# Via UI: Settings → Integrations → Google Calendar
# Requires a Google Cloud Console project
```

### Filter Events

```yaml
automation:
  - alias: "Work meetings only"
    id: work_meetings_only
    trigger:
      - platform: calendar
        entity_id: calendar.work_calendar
        event: start
    condition:
      # Filter on event properties
      - condition: template
        value_template: >
          {{ trigger.calendar_event.summary is not none and
             'meeting' in trigger.calendar_event.summary.lower() }}
    action:
      - service: light.turn_on
        target:
          entity_id: light.office
```

## Workday Sensor

Combine with the workday sensor:

```yaml
# configuration.yaml
binary_sensor:
  - platform: workday
    country: SE
    workdays: [mon, tue, wed, thu, fri]
    excludes: [sat, sun, holiday]
```

```yaml
automation:
  - alias: "Workday morning"
    id: workday_morning
    trigger:
      - platform: time
        at: "07:00:00"
    condition:
      - condition: state
        entity_id: binary_sensor.workday_sensor
        state: "on"
      # Not on vacation
      - condition: state
        entity_id: calendar.vacation
        state: "off"
    action:
      - service: scene.turn_on
        target:
          entity_id: scene.workday_morning
```

## Public Holidays

### Public Holiday Calendar

```yaml
# Use the ICS integration with a public holiday calendar
# or the workday sensor with country: SE
```

### Customize Automations for Public Holidays

```yaml
automation:
  - alias: "Holiday - Sleep in"
    id: holiday_sleep_in
    trigger:
      - platform: time
        at: "09:00:00"  # Later than usual
    condition:
      - condition: or
        conditions:
          # Saturday or Sunday
          - condition: time
            weekday:
              - sat
              - sun
          # Or public holiday
          - condition: state
            entity_id: binary_sensor.workday_sensor
            state: "off"
    action:
      - service: scene.turn_on
        target:
          entity_id: scene.weekend_morning
```

## Meeting Room Booking

```yaml
automation:
  - alias: "Meeting room - Prepare"
    id: meeting_room_prepare
    trigger:
      - platform: calendar
        entity_id: calendar.meeting_room
        event: start
        offset: "-00:05:00"
    action:
      - parallel:
          - service: light.turn_on
            target:
              entity_id: light.meeting_room
            data:
              brightness_pct: 100
          - service: climate.set_temperature
            target:
              entity_id: climate.meeting_room
            data:
              temperature: 22
          - service: media_player.turn_on
            target:
              entity_id: media_player.meeting_room_display

  - alias: "Meeting room - Clean up"
    id: meeting_room_cleanup
    trigger:
      - platform: calendar
        entity_id: calendar.meeting_room
        event: end
        offset: "00:05:00"  # 5 min after
    action:
      - service: light.turn_off
        target:
          entity_id: light.meeting_room
      - service: climate.set_preset_mode
        target:
          entity_id: climate.meeting_room
        data:
          preset_mode: away
```

## Example Prompts

```
"Create an automation that activates vacation mode from the calendar"

"I want a reminder the evening before waste collection"

"Configure dynamic lighting based on calendar events"

"Create a work schedule with different scenes throughout the day"

"Integrate Google Calendar with my home automation"
```

## Best Practices

1. **Use Local Calendar** for schedules you control
2. **Offset triggers** for preparation/cleanup
3. **Combine with workday** for public holiday handling
4. **Filter events** with template conditions
5. **Backup** - Export calendar regularly
6. **Naming convention** - Consistent event names for filtering

## See Also

- `references/automations.md` - Automation patterns
- `references/triggers-advanced.md` - Advanced triggers
- `references/conditions.md` - Conditions
- `references/presence-detection.md` - Presence + calendar
