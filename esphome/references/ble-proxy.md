# BLE Proxy

Guide for configuring ESP32 as a Bluetooth Low Energy (BLE) proxy for Home Assistant.

## Overview

BLE Proxy extends Home Assistant's Bluetooth range by placing ESP32 devices strategically throughout the home. Each proxy forwards BLE advertisements to Home Assistant via the ESPHome API.

## Use Cases

- **Xiaomi/Mijia sensors** - Temperature, humidity, plants
- **iBeacons** - Presence detection
- **Tile/AirTag** - Tracking
- **Bluetooth thermometers** - Kitchen, grill, pool
- **BLE locks** - Smart locks

## Hardware Requirements

| Chip | BLE Proxy | Active Connections | Recommendation |
|------|-----------|-------------------|----------------|
| ESP32 | ✅ Yes | 3 | Standard, cheap |
| ESP32-S3 | ✅ Yes | 9 | Better performance |
| ESP32-C3 | ✅ Yes | 3 | Compact |
| ESP32-C6 | ✅ Yes | 3 | Thread + BLE |
| ESP8266 | ❌ No | - | Lacks BLE |

## Minimal BLE Proxy

```yaml
# Generated by aurora@aurora-smart-home (esphome skill) v1.7.6
# https://github.com/tonylofgren/aurora-smart-home

esphome:
  name: ble-proxy-living-room
  friendly_name: "BLE Proxy Living Room"

esp32:
  board: esp32dev
  framework:
    type: esp-idf  # Recommended for BLE

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

api:

ota:
  platform: esphome

logger:

# BLE Proxy - Forwards to Home Assistant
esp32_ble_tracker:
  scan_parameters:
    active: true
    interval: 1100ms
    window: 1100ms

bluetooth_proxy:
  active: true  # Allows active connections
```

## Optimized BLE Proxy

```yaml
# Generated by aurora@aurora-smart-home (esphome skill) v1.7.6
# https://github.com/tonylofgren/aurora-smart-home

esphome:
  name: ble-proxy-optimized
  friendly_name: "BLE Proxy Optimized"

esp32:
  board: esp32dev
  framework:
    type: esp-idf

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  # Static IP for stability
  manual_ip:
    static_ip: 192.168.1.50
    gateway: 192.168.1.1
    subnet: 255.255.255.0

api:
  encryption:
    key: !secret api_encryption_key

ota:
  platform: esphome
  password: !secret ota_password

logger:
  level: WARN  # Reduce logging for performance

# Optimized BLE settings
esp32_ble_tracker:
  scan_parameters:
    active: true
    interval: 1100ms
    window: 1100ms
    continuous: true

bluetooth_proxy:
  active: true
  cache_services: true  # Caches GATT services
```

## BLE Proxy with Local Sensors

Combine proxy with local Bluetooth sensors:

```yaml
# Generated by aurora@aurora-smart-home (esphome skill) v1.7.6
# https://github.com/tonylofgren/aurora-smart-home

esphome:
  name: ble-proxy-with-sensors
  friendly_name: "BLE Proxy with Sensors"

esp32:
  board: esp32dev
  framework:
    type: esp-idf

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

api:
ota:
  platform: esphome
logger:

esp32_ble_tracker:
  scan_parameters:
    active: true

bluetooth_proxy:
  active: true

# Xiaomi LYWSD03MMC (custom firmware)
sensor:
  - platform: xiaomi_lywsd03mmc
    mac_address: "A4:C1:38:XX:XX:XX"
    bindkey: "YOUR_BIND_KEY"
    temperature:
      name: "Bedroom Temperature"
    humidity:
      name: "Bedroom Humidity"
    battery_level:
      name: "Bedroom Sensor Battery"

  # Xiaomi Mi Flora (plant sensor)
  - platform: xiaomi_hhccjcy01
    mac_address: "C4:7C:8D:XX:XX:XX"
    temperature:
      name: "Plant Temperature"
    moisture:
      name: "Plant Soil Moisture"
    illuminance:
      name: "Plant Light"
    conductivity:
      name: "Plant Conductivity"
    battery_level:
      name: "Plant Sensor Battery"
```

## BLE Presence Detection

Detect presence via BLE devices (phones, smartwatches):

```yaml
# Generated by aurora@aurora-smart-home (esphome skill) v1.7.6
# https://github.com/tonylofgren/aurora-smart-home

esphome:
  name: ble-presence
  friendly_name: "BLE Presence"

esp32:
  board: esp32dev
  framework:
    type: esp-idf

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

api:
ota:
  platform: esphome
logger:

esp32_ble_tracker:
  scan_parameters:
    active: false  # Passive scanning is sufficient
    interval: 1100ms
    window: 1100ms

bluetooth_proxy:
  active: false  # Tracking only

# iBeacon presence
binary_sensor:
  - platform: ble_presence
    ibeacon_uuid: "YOUR-IBEACON-UUID"
    name: "iPhone Home"

  # MAC-based (less reliable)
  - platform: ble_presence
    mac_address: "AA:BB:CC:DD:EE:FF"
    name: "Smartwatch Home"
    timeout: 5min

# RSSI for distance
sensor:
  - platform: ble_rssi
    mac_address: "AA:BB:CC:DD:EE:FF"
    name: "Phone RSSI"
    filters:
      - exponential_moving_average:
          alpha: 0.3
          send_every: 1
```

## Multiple BLE Proxies

For large homes, place multiple proxies:

```
┌─────────────────────────────────────────┐
│                 Home                    │
│                                          │
│  ┌─────┐        ┌─────┐        ┌─────┐  │
│  │ BLE │        │ BLE │        │ BLE │  │
│  │Proxy│        │Proxy│        │Proxy│  │
│  │ #1  │        │ #2  │        │ #3  │  │
│  └──┬──┘        └──┬──┘        └──┬──┘  │
│     │              │              │      │
│     └──────────────┼──────────────┘      │
│                    │                     │
│              ┌─────┴─────┐               │
│              │   Home    │               │
│              │ Assistant │               │
│              └───────────┘               │
└─────────────────────────────────────────┘
```

**Naming convention:**
- `ble-proxy-living-room`
- `ble-proxy-bedroom`
- `ble-proxy-garage`

## ESP-IDF vs Arduino Framework

| Feature | Arduino | ESP-IDF |
|---------|---------|---------|
| BLE Stability | Good | Very good |
| Active Connections | 3 | 3-9 (chip-dependent) |
| Memory Usage | Higher | Lower |
| Compilation Time | Faster | Slower |
| Recommendation | Simple projects | BLE Proxy |

**Use ESP-IDF for BLE Proxy:**
```yaml
esp32:
  board: esp32dev
  framework:
    type: esp-idf
```

## Scan Parameters

| Parameter | Value | Description |
|-----------|-------|-------------|
| `interval` | 1100ms | Time between scans |
| `window` | 1100ms | Time per scan |
| `active` | true/false | Active querying |
| `continuous` | true/false | Continuous scanning |

**Rules of thumb:**
- `window` ≤ `interval`
- `window = interval` for maximum coverage
- `active: true` for devices that require it
- `continuous: true` for presence detection

## Troubleshooting

### Device not visible

1. **Check MAC address** - Verify with nRF Connect app
2. **Enable active scanning** - Some devices require it
3. **Increase window** - Longer scanning period
4. **Check range** - BLE has ~10m range

### Unstable connection

1. **Use ESP-IDF** framework
2. **Reduce number of active connections**
3. **Increase scan interval**
4. **Check WiFi interference** (2.4GHz)

### High CPU/memory usage

```yaml
esp32_ble_tracker:
  scan_parameters:
    active: false  # Passive if possible
    interval: 2000ms  # Increase interval
    window: 1000ms
```

## Home Assistant Integration

BLE Proxy integrates automatically:

1. **Install ESPHome integration** in HA
2. **Adopt the device** when it is discovered
3. **BLE devices appear** under Integrations - Bluetooth

**No extra configuration needed** - Home Assistant sees all BLE devices via the proxy.

## Example Prompts

```
"Configure ESP32 as BLE proxy for my Xiaomi sensors"

"I want presence detection with iBeacon and ESP32"

"Create a BLE proxy with Xiaomi Mi Flora plant sensor"

"Optimize my BLE proxy for better range and stability"

"Configure multiple BLE proxies for my two-story house"
```

## See Also

- `references/bluetooth.md` - General Bluetooth configuration
- `references/sensors.md` - Sensor platforms
- `references/power-management.md` - Battery-powered devices
