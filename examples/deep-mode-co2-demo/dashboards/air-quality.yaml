# Generated by aurora@aurora-smart-home (home-assistant skill)
# https://github.com/tonylofgren/aurora-smart-home
#
# DEEP-mode demo: this is the artifact Iris recorded in aurora-project.json
# (ha_yaml_files: dashboards/air-quality.yaml). Iris briefly raised a
# conflict because sensor.co2_monitor_humidity was not yet listed in
# entity_ids_generated; once Sage confirmed it, Iris finished.

title: Air Quality
views:
  - title: Office
    cards:
      - type: gauge
        entity: sensor.co2_monitor_co2
        name: CO2
        unit: ppm
        min: 400
        max: 2000
        severity:
          green: 400
          yellow: 800
          red: 1200

      - type: history-graph
        title: CO2 last 24h
        hours_to_show: 24
        entities:
          - entity: sensor.co2_monitor_co2

      - type: entities
        title: Environment
        entities:
          - entity: sensor.co2_monitor_temperature
          - entity: sensor.co2_monitor_humidity
