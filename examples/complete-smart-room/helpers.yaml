# Generated by ha-yaml@aurora-smart-home v1.0.0
# https://github.com/tonylofgren/aurora-smart-home
#
# Smart Room Helpers
# ------------------
# Input helpers for room mode management and settings.

# =============================================================================
# Room Mode Selection
# =============================================================================

input_select:
  office_mode:
    name: "Office Mode"
    icon: mdi:home-variant
    options:
      - work
      - relax
      - movie
      - night
      - away
    initial: work

# =============================================================================
# Settings Toggles
# =============================================================================

input_boolean:
  office_auto_brightness:
    name: "Office Auto Brightness"
    icon: mdi:brightness-auto

  office_presence_lighting:
    name: "Office Presence Lighting"
    icon: mdi:motion-sensor
    initial: true

  office_climate_control:
    name: "Office Climate Control"
    icon: mdi:thermostat
    initial: true

# =============================================================================
# Climate Settings
# =============================================================================

input_number:
  office_target_temperature:
    name: "Office Target Temperature"
    icon: mdi:thermometer
    min: 16
    max: 28
    step: 0.5
    unit_of_measurement: "°C"
    initial: 22

  office_night_temperature:
    name: "Office Night Temperature"
    icon: mdi:thermometer-low
    min: 16
    max: 24
    step: 0.5
    unit_of_measurement: "°C"
    initial: 20

# =============================================================================
# Timers
# =============================================================================

timer:
  office_presence_timeout:
    name: "Office Presence Timeout"
    icon: mdi:timer-outline
    duration: "00:30:00"
