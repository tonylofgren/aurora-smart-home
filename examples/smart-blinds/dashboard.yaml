# Generated by aurora@aurora-smart-home (esphome skill)
# https://github.com/tonylofgren/aurora-smart-home
#
# Smart Blinds Lovelace Dashboard

title: Blinds
views:
  - title: Blinds
    path: blinds
    icon: mdi:roller-shade
    cards:
      - type: vertical-stack
        cards:
          - type: markdown
            content: >
              ## Smart Blinds

          - type: tile
            entity: cover.blinds
            features:
              - type: cover-open-close
              - type: cover-position

      - type: horizontal-stack
        cards:
          - type: button
            entity: cover.blinds
            name: Open
            icon: mdi:arrow-up-bold
            tap_action:
              action: call-service
              service: cover.open_cover
              target:
                entity_id: cover.blinds
            show_state: false

          - type: button
            entity: cover.blinds
            name: Stop
            icon: mdi:stop
            tap_action:
              action: call-service
              service: cover.stop_cover
              target:
                entity_id: cover.blinds
            show_state: false

          - type: button
            entity: cover.blinds
            name: Close
            icon: mdi:arrow-down-bold
            tap_action:
              action: call-service
              service: cover.close_cover
              target:
                entity_id: cover.blinds
            show_state: false

      - type: entities
        title: Endstops
        entities:
          - entity: binary_sensor.blinds_endstop_open
            name: Open Limit
          - entity: binary_sensor.blinds_endstop_closed
            name: Closed Limit

      - type: entities
        title: Device
        entities:
          - entity: binary_sensor.blinds_status
            name: Online
          - entity: sensor.blinds_wifi
            name: WiFi Signal
          - entity: button.blinds_calibrate
            name: Run Calibration
          - entity: button.blinds_restart
            name: Restart Device
